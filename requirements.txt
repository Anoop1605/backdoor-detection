# ============================================
# Backdoor Detection System - Requirements
# ============================================

# ============================================
# CORE DEPENDENCIES (Required for all modes)
# ============================================
flask>=2.0.0
psutil>=5.8.0

# ============================================
# PRODUCTION MODE - ML & Detection
# ============================================
# Uncomment for production deployment with real detection

# Deep Learning
tensorflow>=2.10.0,<2.16.0
keras>=2.10.0

# Data Science & ML
pandas>=1.3.0
numpy>=1.21.0,<2.0.0
scikit-learn>=1.0.0
joblib>=1.1.0

# ============================================
# ALERT SYSTEM (Optional)
# ============================================
# Uncomment based on your alert channels

# Email Alerts
# (built-in with Python smtplib)

# SMS Alerts (Twilio)
# twilio>=8.0.0

# HTTP Requests (for webhooks)
requests>=2.28.0

# ============================================
# DEVELOPMENT & TESTING (Optional)
# ============================================
# Uncomment for development and testing

# Testing
# pytest>=7.0.0
# pytest-cov>=4.0.0

# Code Quality
# pylint>=2.15.0
# black>=22.0.0
# flake8>=5.0.0

# ============================================
# SYSTEM DEPENDENCIES (Install via apt/yum)
# ============================================
# These cannot be installed via pip:
#
# sudo apt install -y suricata          # IDS/IPS engine
# sudo apt install -y nmap              # Port scanning (testing)
# sudo apt install -y hping3            # Network stress testing
# sudo apt install -y netcat            # Network utilities
# sudo apt install -y hydra             # Brute force testing
# sudo apt install -y python3-dev       # Python development headers
# sudo apt install -y build-essential   # Compilation tools

# ============================================
# NOTES
# ============================================
# 
# DEMO MODE:
#   pip install flask psutil
#
# PRODUCTION MODE (Minimal):
#   pip install -r requirements.txt
#   (Uncomment tensorflow, pandas, scikit-learn, etc.)
#
# PRODUCTION MODE (Full):
#   pip install -r requirements.txt
#   (Uncomment all production dependencies)
#
# DEVELOPMENT:
#   pip install -r requirements.txt
#   (Uncomment development dependencies)
#
# For detailed installation instructions, see:
#   - QUICKSTART.md
#   - PRODUCTION_DEPLOYMENT.md
